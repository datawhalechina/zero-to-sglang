# 写作模板

## 文件与图片

- 一章一个 markdown 文件，放在 `docs/partN/` 下，文件名 `第N章_中文标题.md`。
- 图片放在同一 part 的 `docs/partN/images/` 下，文件名 `章号-序号-图片说明.png`，例如 `6-4-SM的架构.png`。
- 图片用 HTML 标签引用，宽度统一 800。

## 标题层级

一级标题全文只有一个，就是章节标题：

```markdown
# 第 N 章 Headline Content
```

二级标题是小节：

```markdown
## 1 balabala

## 2 balabala
```

三级标题编号为「小节号.序号」，四级标题为「小节号.序号.序号」：

```markdown
### 2.1 balabala

### 2.2 balabala

#### 2.2.1 balabala

#### 2.2.2 balabala
```

四级以下不再往下分，还需要分层的地方用无序列表或加粗行。同一层级内编号必须连续，不跳号、不重号。不要用一级标题充当小节标题。

## 开头

一级标题下面直接写一段导语，不加标题，三到五句话，依次说清两件事：

1. 本章讲什么
2. 这部分能学到什么

学习目标清单、章节目录、前置知识清单都不用写（前置要求统一放在 README）。

## 结尾

每章固定用两个二级标题收尾。

第一个是本章最后一节，标题为「总结与测试题」，下面带两个三级标题：

```markdown
## 6 总结与测试题

### 6.1 课程总结

### 6.2 测试题
```

课程总结按本章二级标题的顺序逐条复述结论，一条一段。测试题只出题不给答案。

第二个是参考资料，不参与小节编号：

```markdown
## 参考资料
```

无序列表，一行一条，写清标题和链接，只列真正引用过的资料。
